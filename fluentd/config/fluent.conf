<source>
  @type forward
  port 24224
</source>
<match fluentd.test.**>
  @type stdout
</match>
<match fluentd.test.**>
  @type elasticsearch
  index_name applog
  type_name spring
  host elasticsearch
  port 9200
  logstash_format true
  logstash_prefix applog
</match>